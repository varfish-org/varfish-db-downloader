dc.identifier: varfish-server-worker/masked-repeat/{{ genome_release }}:{{ version }}+{{ v_worker }}
dc.title: "VarFish Server Worker Masked Regions: Repeat Masker Regions"
dc.creator: UCSC Genome Browser Team
dc.contributor:
  - VarFish Developer Teams
dc.format: application/x-protobuf
dc.date: {{ today }}
x-version: {{ version }}
x-genome-release: {{ genome_release }}
dc.description: |
  Protobuf file with UCSC genome browser goldenPath repeat masker output file.
dc.source:
  - https://hgdownload.cse.ucsc.edu/goldenpath
x-created-from:
  - name: UCSC Genome Browser goldenPath "rmsk" file
    version: {{ version }}
x-created-with:
  - name: varfish-db-downloader
    version: {{ v_downloader }}
  - name: varfish-server-worker
    version: {{ v_worker }}
